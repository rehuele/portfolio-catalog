{
  "name": "portfolio-catalog",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "dev": "parcel src/*.html --no-source-maps --open",
    "build": "parcel build src/index.html --no-source-maps --dist-dir public --public-url ./"
  },
  "packageManager": "yarn@3.2.3",
  "devDependencies": {
    "@parcel/transformer-sass": "2.7.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "swiper": "^8.4.2"
  }
}
